# 数据分析平台后端（与 adata 分开安装：pip install -r requirements-platform.txt）
fastapi>=0.110.0
# 生产 systemd 以单进程 uvicorn 启动且不启用 --reload；不要引入
# watchfiles 等开发期可选组件，否则 CPython 3.14 无法构建完整离线轮包锁。
uvicorn==0.52.3
pydantic-settings>=2.0.0
httpx>=0.27.0
pandas>=2.0.0
numpy>=1.26.0
requests>=2.31.0
# adata imports these dependencies during package initialization.  They are
# explicit so the production wheel lock cannot omit them during offline deploys.
beautifulsoup4==4.15.0
py-mini-racer==0.6.0
# 阿里云生产镜像当前可用上限；显式固定，避免 CI 冻结到镜像尚未同步的新版本。
charset-normalizer==3.5.0
idna==3.18
sqlalchemy>=2.0.0
pymysql>=1.1.0
fastavro>=1.9.0
cryptography>=43.0.0
# 可选：仅聚宽分钟数据链路需要；主站应可在未安装时正常启动
jqdatasdk>=1.9.8
# 可选：仅 QMT 盘中数据链路需要；需先安装 QMT 客户端，xtquant 通常随 QMT 自带
# pip install xtquant  或从 QMT 安装目录复制 xtquant 到 site-packages
# xtquant
pytest>=8.0.0
