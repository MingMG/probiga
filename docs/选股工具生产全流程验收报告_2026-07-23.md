# 选股工具生产全流程验收报告

> 验收日期：2026-07-23  
> 验收环境：生产 `http://47.113.123.190`  
> 验收范围：统一选股工作台、七套预设、日期一致性、旧接口兼容、保存方案、候选池、候选中心、静态资源和服务状态

## 1. 验收结论

**结论：有条件通过。**

代码、服务、接口和核心选股流程均通过生产验收；未发现服务不可用、接口 5xx、静态资源缺失或日期向未来泄漏问题。

需要继续关注一项数据准备问题：候选中心当前返回 0 条候选，AI 推荐数据没有形成当日候选，聚宽数据最新日期为 `2026-05-28`，接口已正确标记为陈旧。该问题属于上游推荐/策略数据未更新，不是选股工作台代码故障。

本轮验收过程中发现并修复了一个真实问题：指定历史日期没有龙虎榜数据时，接口原先会错误回退到更新日期；修复后改为返回空结果并标记 `freshness=unavailable`，避免回测或历史研究产生未来数据泄漏。

## 2. 部署与运行基线

| 检查项 | 结果 |
|---|---|
| 首页 | HTTP 200 |
| `probiga` 服务 | `active` |
| 服务最近启动时间 | `2026-07-23 20:34:35 CST` |
| 生产本机 `/api/screener/catalog` | `ok / 7 presets` |
| 最近 10 分钟 error 日志 | `-- No entries --` |
| 生产文件哈希 | 与本地一致 |
| 回滚备份 | `/opt/ProBigA/.codex_backup_screener_final_20260723_203432` |

本轮最终部署文件均已做远端哈希核对：`screener.py`、`hot_data.py`、`main.py`、`app.js`、`style.css`、`index.html`、`screen_stocks.py` 全部匹配。

## 3. 统一选股工作台验收

验收请求统一使用：

```text
as_of_date=2026-07-22
top=5
exclude_st=true
```

| 预设 | HTTP | 耗时 | 结果数 | 实际数据日 | 新鲜度 | 结论 |
|---|---:|---:|---:|---|---|---|
| 趋势突破 `trend_breakout` | 200 | 0.26s | 0 | 2026-07-22 | exact | 通过；条件严格，当前无结果 |
| 启动突破 `startup_breakout` | 200 | 1.69s | 5 | 2026-07-22 | exact | 通过 |
| 低位反转 `oversold_reversal` | 200 | 0.91s | 5 | 2026-07-22 | exact | 通过 |
| 资金承接 `capital_support` | 200 | 1.10s | 5 | 2026-07-22 | exact | 通过 |
| 情绪连板 `momentum_ladder` | 200 | 0.41s | 5 | 2026-07-22 | exact | 通过 |
| 龙虎榜事件 `dragon_tiger` | 200 | 0.04s | 0 | 2026-07-22 | unavailable | 通过；该日期无龙虎榜数据，未向未来日期泄漏 |
| MACD 金叉 `technical_cross` | 200 | 1.68s | 5 | 2026-07-22 | exact | 通过 |

七套预设均能正常返回结构化响应；指定日期下，6 套有可用结果，龙虎榜为数据不可用告警，不属于接口错误。

## 4. 日期一致性验收

| 场景 | 请求日期 | 返回数据日 | 新鲜度 | 结论 |
|---|---|---|---|---|
| 历史精确筛选 | 2026-07-22 | 2026-07-22 | exact | 通过 |
| 未来日期请求 | 2099-01-01 | 2026-07-23 | fallback | 通过，回退到最新已落库日 |
| 龙虎榜历史无数据 | 2026-07-22 | 空 | unavailable | 通过，不使用 2026-07-23 数据 |

这部分重点验证了日期选择器不会被后端静默忽略，也不会在历史查询中混入未来数据。

## 5. 旧接口兼容验收

`/api/hot-data/screen-stocks` 的旧模式全部返回 HTTP 200：

| 模式 | 结果数 | 数据日 | 新鲜度 |
|---|---:|---|---|
| `lhb` | 0 | 空 | unavailable |
| `flow` | 3 | 2026-07-22 | exact |
| `k_day` | 3 | 2026-07-22 | exact |
| `low_start` | 3 | 2026-07-22 | exact |
| `trend` | 3 | 2026-07-22 | exact |
| `trend_strong` | 0 | 2026-07-22 | exact |
| `ladder` | 3 | 2026-07-22 | exact |
| `macd` | 3 | 2026-07-22 | exact |
| `startup` | 3 | 2026-07-22 | exact |

MACD 接口本轮实际返回了计算结果，兼容旧页面调用。

## 6. 保存方案与候选池闭环

使用唯一临时验收数据完成了真实生产闭环，随后清理：

1. 创建临时保存方案：HTTP 200。
2. 查询保存方案：能够读到 1 条临时方案。
3. 删除保存方案：HTTP 200。
4. 创建临时候选：HTTP 200。
5. 查询候选池：能够读到 1 条临时候选。
6. 删除临时候选：HTTP 200。
7. 清理复查：保存方案和候选池均回到 0 条。

验收未留下测试数据。

## 7. 候选中心验收

生产返回：

```text
status=degraded
candidate_count=0
data_date=2026-07-22
jq_latest=2026-05-28
jq_stale=True
```

接口行为符合预期：没有候选时不伪造结果，并把数据源陈旧状态显式返回。后续需要补做推荐任务/聚宽策略数据更新，再复核候选中心是否恢复为 `status=ok`。

## 8. 页面与静态资源验收

| 资源 | HTTP | 大小 | 关键标记 |
|---|---:|---:|---|
| 首页 | 200 | 通过 | 包含“选股工作台”“候选中心” |
| `/static/js/app.js` | 200 | 738,626 bytes | 包含 `screener` |
| `/static/css/style.css` | 200 | 97,473 bytes | 包含工作台样式 |

本地 `node --check server/static/js/app.js` 通过；生产浏览器自动化尝试被客户端对该 IP 的访问策略拦截（`ERR_BLOCKED_BY_CLIENT`），因此本报告的页面验收以生产首页、静态资源和接口结果为准，未把浏览器拦截误判为应用故障。

## 9. 本地质量检查

| 检查项 | 结果 |
|---|---|
| 选股专项测试 | 15 passed |
| Python 编译检查 | 通过 |
| JavaScript 语法检查 | 通过 |
| 全量测试 | 673 passed, 2 failed, 11 subtests passed |

全量测试失败的两项均来自既有国金 QMT 定时任务配置：

- `tests/test_qmt_reconciliation.py::test_qmt_nightly_reconciliation_task_is_registered`
- `tests/test_qmt_reconciliation.py::test_qmt_catalog_refresh_task_is_registered`

失败原因是当前任务 `enabled=0`，测试期望 `enabled=1`；本次未修改 QMT 范围，避免把无关配置混入选股发布。

## 10. 后续建议

1. 优先恢复 AI 推荐任务和聚宽策略数据更新，确保候选中心有新鲜候选。
2. 在下一交易日收盘后复核：候选中心应出现候选，且聚宽最新日期不应继续停留在 `2026-05-28`。
3. 另行确认两个 QMT 定时任务的启用策略，并同步修正配置或测试预期。
4. 若需要真正的视觉回归，可在允许访问生产 IP 的浏览器环境中补一次截图验收；本轮接口和静态资源证据已足以确认发布版本正确运行。

## 11. 最终判定

**选股工具代码与生产服务：通过。**  
**候选中心数据完整性：有条件通过，等待上游推荐/聚宽数据恢复后复核。**
