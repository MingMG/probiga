# 国金 QMT 接入执行清单

更新时间：2026-06-27

状态说明：`[ ]` 未开始，`[-]` 进行中，`[x]` 已完成，`[!]` 阻塞或等待权限。

## 0. 执行与交付规则

- [x] 以迅投知识库的股票、行业概念、指数数据字典为接口基准。
- [x] 股票、行业概念、指数业务数据源统一标识为 `gj_qmt`。
- [x] 保留热股排行原有渠道及融合逻辑。
- [x] 保留快讯原有采集渠道和主体页面结构。
- [x] 自动买卖仅做模拟交易，不向真实证券账户下单。
- [x] 每批交付列明：本次完成项、验证结果、未完成项、下一步。
- [x] 基础数据未通过验收前，不提前启用依赖数据的推荐与交易逻辑。
- [x] 历史数据缺口后续通过补数队列和深度轮转补齐，不阻塞当前增量接入和基础链路建设。
- [x] 大体量历史数据写入本地历史库，不写入生产业务库。

## 1. 国金 QMT 运行环境

- [x] 修复国金 QMT 客户端路径发现逻辑。
- [x] 增加 `GJ_QMT_HOME`、`GJ_QMT_EXE`、`GJ_QMT_PROVIDER_ID` 配置。
- [x] 检查国金 QMT 客户端是否运行。
- [x] 检查 SDK Python 是否存在并可导入官方模块。
- [x] 检查 MiniQMT 行情连接是否成功。
- [x] 增加连接超时、断线重连和错误分类。
- [x] 将健康接口改为真实检查客户端、SDK和QMT连接。
- [x] 建立官方接口存在性和权限探测基础。
- [x] 输出客户端版本、SDK版本和成功连接端口。
- [ ] 完成国金 QMT 自动启动与开机运行验证。
- [x] 将当前一次一进程调用改为常驻 QMT 网关，并保留子进程回退。

## 2. 官方接口台账

- [x] 创建 `qmt_api_registry`。
- [x] 创建 `qmt_api_capability`。
- [-] 登记股票概况全部接口。
- [-] 登记股票行情全部周期和字段。
- [-] 登记 Level-2、资金流、订单流接口。
- [-] 登记交易日历、龙虎榜、北向南向数据。
- [-] 登记公告、问董秘接口。
- [-] 登记财务报表全部表和字段。
- [-] 登记行业概念下载、目录、成分接口。
- [-] 登记指数列表、成分、权重、行情接口。
- [-] 对每个接口记录真实权限状态和返回字段。

## 3. 原始审计层与数据库迁移

- [x] 创建 `qmt_raw_manifest`。
- [x] 建立按数据集和日期分区的原始文件目录。
- [x] 保存接口参数、SDK版本、客户端版本和数据哈希。
- [x] 增加数据库断线本地待写队列。
- [-] 给业务表增加 `qmt_code`。
- [-] 给业务表增加 `data_source`。
- [-] 给业务表增加 `source_time`、`received_at`。
- [-] 给业务表增加 `batch_id`、`data_version`。
- [-] 给业务表增加 `quality_status`、`permission_status`。
- [-] 清理重复数据后建立业务唯一索引。
- [-] 所有同步改为临时表校验后 Upsert。
- [-] 禁止抓取前清空正式表。

## 4. 股票数据

- [ ] 股票代码与完整合约信息。
- [ ] 上市、退市、当前状态。
- [ ] 当前与历史 ST、*ST、PT 状态。
- [ ] 总股本、流通股本、除权因子。
- [ ] Tick、实时快照、五档盘口。
- [ ] 1/5/15/30/60 分钟行情。
- [ ] 日、周、月行情及全部复权方式。
- [ ] 资金流和逐笔成交统计。
- [ ] 订单流及 Level-2 授权数据。
- [ ] 交易日历。
- [ ] 龙虎榜。
- [ ] 北向南向资金和持股。
- [ ] 交易所公告、问董秘。
- [ ] 财务数据全部表和字段。

## 5. 行业概念与指数

- [ ] 下载当前和历史板块分类。
- [ ] 全部行业概念目录。
- [ ] 当前及历史板块成分关系。
- [ ] 真实 QMT `.BKZS` 板块指数识别。
- [ ] 板块指数实时、分钟和历史行情。
- [ ] 全部指数代码与完整合约信息。
- [ ] 指数当前及历史成分股。
- [ ] 指数成分权重。
- [ ] 指数实时、Tick、分钟和历史行情。
- [ ] 移除或隔离冒充官方价格的基准 1000 合成指数。

## 6. 数据质量与自动补数

- [x] 创建 `sys_data_sync_run`。
- [x] 创建 `sys_data_coverage`。
- [x] 创建 `sys_data_gap`。
- [x] 创建 `sys_data_quality_result`。
- [-] 实现字段、类型、唯一性和时间校验。
- [-] 实现 OHLC、成交量、成交额校验。
- [ ] 实现交易日和停牌例外校验。
- [ ] 实现 Tick、分钟、日线的 QMT 内部一致性校验。
- [x] 建立每天 01:30 的 `nightly_data_reconciliation`。
- [x] 最近 20 个交易日详细扫描。
- [-] 历史缺口队列持续修复。
- [ ] 周末历史深度轮转扫描。
- [-] 失败重试、幂等锁和未解决缺口报警。

## 7. 盘中实时页面

- [ ] 常驻行情订阅池。
- [ ] 自选股、持仓股、推荐股动态订阅。
- [ ] WebSocket 实时推送。
- [ ] 默认 3 秒刷新。
- [ ] 1 秒极速模式。
- [ ] 手动实时刷新。
- [ ] 市场概览实时刷新。
- [ ] 展示资金流、盘口、数据时间和延迟状态。
- [ ] 趋势、资金、热度、建议和风险分析。

## 8. AI 推荐

- [ ] 建立推荐运行批次和输入快照。
- [ ] 08:30 自动运行。
- [ ] 严格计算上一个交易日。
- [ ] 推荐前自动补数并复检。
- [ ] 数据不完整时终止，不回退旧日期。
- [ ] 新闻截止时间固定为执行时刻。
- [ ] 推荐页面增加时间范围等筛选。
- [ ] 保存入场、止损、止盈、仓位和失效条件。

## 9. 模拟交易与回测

- [ ] 建立 100 万元模拟账户。
- [ ] 现金、冻结、市值、总资产完整记账。
- [ ] 单股、行业、总仓位限制。
- [ ] T+1、涨跌停、停牌、手续费、印花税和滑点。
- [ ] AI 推荐与盘中信号双确认。
- [ ] 完整模拟委托和成交记录。
- [ ] 新增独立回测页面。
- [ ] 每次回测使用独立 `run_id`。
- [ ] 不再删除上一轮回测结果。
- [ ] 多策略独立资金及组合资金模式。
- [ ] 净值、回撤、收益、夏普、胜率、盈亏比等指标。
- [ ] 防未来函数和幸存者偏差。

## 10. 保留模块与旁路增强

- [ ] 验证原热股排行渠道和融合结果不受影响。
- [ ] 新增独立国金 QMT 活跃榜，不自动并入融合榜。
- [ ] 验证快讯原采集渠道不受影响。
- [ ] 快讯重要性和影响分析采用旁路处理。
- [ ] 新闻分析失败不阻塞原文入库。

## 11. 最终联调与验收

- [ ] 单元测试与接口契约测试通过。
- [ ] MySQL 断线重放测试通过。
- [ ] QMT客户端断线重连测试通过。
- [ ] 凌晨补数幂等测试通过。
- [ ] 08:30 数据门禁测试通过。
- [ ] 模拟交易和回测可重复性测试通过。
- [ ] 至少完成一个完整交易日联调。
- [ ] 建议完成连续两个交易日稳定性验证。

## 交付记录

### 批次 1：国金 QMT 运行基础

- 状态：已完成
- 本次目标：客户端路径、真实连接健康检查、接口能力探测骨架及对应测试。
- 完成结果：
  - 国金 QMT 客户端 `2.0.8.300` 已识别并运行。
  - SDK 已通过端口 `58610` 连接。
  - 常驻HTTP网关已运行在 `127.0.0.1:18765`。
  - 51 条接口/周期定义和 40 条能力记录已落库。
  - 27 个原生函数存在，10 项核心数据真实返回。
  - `get_market_time` 当前原生 SDK 不存在。
  - `get_trading_calendar` 当前客户端返回功能未实现。
  - QMT板块指数示例目录当前无数据，等待参考资料下载能力处理。
  - 17 项相关自动化测试通过。

### 批次 2：官方字段台账与原始审计层

- 状态：进行中
- 本次目标：补齐字段级接口定义、原始批次清单、同步运行记录和业务表安全迁移基础。
- 已完成结果：
  - 已创建 `qmt_raw_manifest`、`sys_data_sync_run`、`sys_data_coverage`、`sys_data_gap`、`sys_data_quality_result` 5 张表。
  - 已实现按数据集和北京时间日期分区的 gzip 原始数据归档。
  - 每份归档保存真实接口参数、客户端版本、SDK版本、连接端口、传输方式及 SHA-256。
  - 已用真实 QMT 能力探测与核心数据探测完成 2 份归档，数据库状态均为 `SUCCESS`，文件及哈希复核一致。
  - 已增加数据库断线本地待写队列；原始文件写入成功但数据库 manifest 写入失败时，不删除原始文件，改为生成本地待写记录。
  - 已增加 `tools/replay_guojin_qmt_pending_writes.py`，用于数据库恢复后重放待写 manifest。
  - 已增加 `tools/setup_guojin_qmt_business_schema.py`，业务表 QMT 溯源字段支持 dry-run 和指定表 apply。
  - 迁移工具默认 apply 时会跳过 `sm_stock_kline`、`sm_stock_minute` 两张大表，需显式指定才会执行，避免误锁表。
  - 已对 13 张现有业务表追加 `qmt_code`、`data_source`、`source_time`、`received_at`、`batch_id`、`data_version`、`quality_status`、`permission_status`。
  - `sm_stock_kline`、`sm_stock_minute` 因 MySQL 5.5.20 不支持 instant add column，且表规模分别约 756 万行/3.2GB、522 万行/1.35GB，需维护窗口或在线迁移方案，未继续强行锁表。
  - 已增加 QMT 专用安全 Upsert 底座：先写临时表校验，再执行 `ON DUPLICATE KEY UPDATE`，不执行 `TRUNCATE` 或整表删除。
  - 已为 `sm_stock_current(stock_code)` 建立唯一索引 `uk_qmt_sm_stock_current_code`，用于实时行情安全 Upsert。
  - 已将 `tools/sync_qmt_realtime.py` 的当前行情写入从 `DELETE/TRUNCATE + append` 改为安全 Upsert。
  - 已用国金 QMT 对 `000001` 完成一次真实当前行情 Upsert 验证，写入 `qmt_code=000001.SZ`、`data_source=gj_qmt` 和同步批次。
  - 历史数据缺口已明确后续通过补数队列持续补齐，不作为当前增量链路阻塞项。
  - 本批相关自动化测试当前 34 项通过。
- 尚未完成：字段级完整台账、两张大行情表字段迁移、把具体 QMT 同步任务逐个切到安全 Upsert。

### 批次 3：安全入库、凌晨扫描与历史缺口队列

- 状态：已完成可安全落地部分；历史补数执行保持后续补。
- 本次目标：QMT 当前行情安全 Upsert、调度注册、凌晨缺口扫描、历史缺口队列。
- 完成结果：
  - 已新增 `integrations/qmt/safe_upsert.py`，所有新 QMT 业务写入可走“临时表校验 + `ON DUPLICATE KEY UPDATE`”，不清空正式表。
  - 已为 `sm_stock_current(stock_code)` 创建唯一索引 `uk_qmt_sm_stock_current_code`。
  - 已将 `tools/sync_qmt_realtime.py` 改为安全 Upsert，并用国金 QMT 对 `000001` 完成真实写入验证。
  - 已新增 `tools/simulate_qmt_intraday_realtime.py`，用于非交易时间模拟盘中 1 秒/3 秒刷新联调。
  - 已修复 QMT 当前行情 `timetag` 带小数秒时的解析问题，例如 `20260626 15:00:00.1`。
  - 已完成 5 只股票 3 轮模拟刷新：每轮请求 5、返回 5、覆盖率 100%，安全 Upsert 正常写入 `sm_stock_current`。
  - 已重启国金 QMT 常驻网关，并用网关模式完成 5 只股票验证：1 轮 425ms，覆盖率 100%。
  - 已新增 `integrations/qmt/reconciliation.py` 和 `tools/nightly_guojin_qmt_reconciliation.py`。
  - 已注册 `国金QMT凌晨缺口扫描`：每天 `01:30` 执行，扫描最近 20 个交易日。
  - 已注册 `国金QMT历史缺口修复队列`：每天 `02:00` dry-run 输出待修复缺口，暂不自动写历史数据。
  - 已注册 `国金QMT盘中实时行情同步`：交易时段从 `09:25` 起每 1 分钟独立运行，不影响原热股排行和快讯。
  - 已真实执行最近 20 个交易日扫描：写入 `sys_data_coverage` 40 条，当前打开历史缺口 25 个，质量结果 4 条。
  - 断线待写队列重放链路已纳入凌晨扫描，当前待写 0 条。
  - QMT 相关自动化测试当前 40 项通过。
- 尚未完成：
  - 历史缺口的真实自动补数执行器尚未接入；需要在 QMT 官方历史下载接口、业务唯一索引和大表迁移验收后开启。
  - `sm_stock_kline`、`sm_stock_minute` 两张大表仍需维护窗口或在线迁移方案补齐 QMT 溯源字段。
  - 字段级官方接口台账仍需继续按国金 QMT 文档逐项核验。

### 批次 4：本地历史库补数脚本

- 状态：已完成脚本与小样本真实写入验证。
- 本次目标：历史数据量过大，写入本地库，不进入生产业务库。
- 完成结果：
  - 已新增配置 `QMT_HISTORY_MYSQL_URL`；如未配置，会拒绝回退到生产 `MYSQL_URL`。
  - 已新增本地历史补数模块 `integrations/qmt/local_history.py`。
  - 已新增命令行脚本 `tools/backfill_guojin_qmt_local_history.py`。
  - 本地历史库表：
    - `qmt_local_stock_kline`
    - `qmt_local_stock_minute`
    - `qmt_local_backfill_run`
  - 已在本机 MySQL 创建独立库 `probiga_qmt_history`。
  - 已用国金 QMT 真实写入小样本：
    - `000001` / `2026-06-26` 日线 1 条。
    - `000001` / `2026-06-26` 1分钟线 241 条。
  - 所有写入均在 `probiga_qmt_history`，未写入生产业务库 `probiga`。
- 尚未完成：
  - 全市场、全历史范围尚未开始批量跑；需要按批次逐步执行，避免 QMT 下载和本机数据库压力过大。
  - 资金流、Level-2、指数、行业概念历史补数脚本仍需按官方接口继续扩展。
