# Windows 更新器：按授权版本推进

## 2026-09-05 实际发布后补充（待部署）

- `43235fd` 两端已生效；参考目录真实刷新用时 132 秒，5558 只股票、595 条价格指数。龙虎榜随后真实写入 65 条，但后置验证仍直接调用 Git，已统一复用发布身份解析器。
- 真实数据回放发现调度器资金流来源名单遗漏 `push2hist`，以及在已验证沪深提供方精确分区后，又错误要求不支持的北交所资金流。现在保留 K 线/目录一致性检查，资金流任务只确认已支持的提供方范围；下游全市场分析的完整性要求不放宽。真实 5207 条资金流回执通过完整只读验收。
- 指数及 ETF 的后置版本判断仍把应用提交号等同于已加载策略提交号。现在使用既有内容兼容证明，保留真实策略版本和内容哈希。指数 595/595 条真实日线及 15 个响应回执已通过只读验证，尚待新版本正式入库。
- 历史补数已将 9 月 3 日资金流从 0 保存到 5123 条，随后板块热度阶段在 20 分钟任务上限退出。旧 Node 不支持全局 fetch/AbortController；备用请求改用原生 HTTP(S) 和整轮请求定时器，不增加依赖或取消 TLS 校验。断点继续保留，不宣称历史缺口已全部补齐。
- 财务 NaT 问题的四只股票生产重跑成功，剩余 002731 缺少 2026-06-30 正式财报或可信未披露证据，不伪造豁免。
- 本轮定向回归 170 passed；包括真实本机 HTTP 请求在禁用 fetch/AbortController 下成功、生产龙虎榜后置验证不调用 Git、未知来源与篡改策略身份仍被拒绝。生产 Node 已只读确认是 v12.22.9。差异 review 未新增服务、数据库结构或任务并发。

## 2026-09-05 后续实测定位与修复

- `eca465f` 已在两端运行，API 正常且 Windows READY。ETF 14 只、两种复权共 28 条实际写入 PASS；任务整体仍因禁止历史回填模拟账本而标记失败，不能混称行情采集失败。
- 指数实际目录有 609 条。交易日历已使用交易所官方批次，不能要求它等于 QMT 目录批次；目录写入工具实际使用 `gj_qmt`，需与读取端统一。现在分别绑定真实批次，目录仍须归属已有不可变 QMT 参考批次，并记录元数据摘要。428 条指数缺少起始日期时保持未知，不填造日期，仍要求目标日真实行情。
- 对 609 个原代码只读请求得到 575 条有效日线。其余 20 个 `98xxxx` 国证指数被映射成 `.SH`，真实 QMT `.SZ` 请求可返回行情；14 个 `395xxx` 则是深交所成交量统计项，并非价格指数。修正映射并从价格指数范围排除统计项，不删除原始统计数据。依据：[深交所行情接口技术文档](https://www.szse.cn/www/marketServices/technicalservice/guide/P020230904549038313109.pdf)、[国证指数目录](https://www.cnindex.com.cn/zh_indices/cni/scale/index.html?act_menu=0&index_type=200)。
- 龙虎榜 `920071` 的完整 QMT 原生信息为金钛股份、2026-09-03 上市；已有股票目录停在 2026-09-02。日常参考任务新增 `--catalog-only`：继续采集股票、指数和日历，不等待全量板块成分或指数权重；原完整模式仍可使用，相关旧表不被清空。只读试跑 130 秒，发现 5,558 只当前证券，包含新上市代码；尚需部署后的真实目录刷新。
- 常规资金流读取错误地把不受支持的北交所纳入补数范围，且不接受另一现有采集器写入的 `push2hist` 来源名，导致已完整的 5,207 条也重新抓取。修正后生产只读复用 5,207 条耗时约 1 秒，0 次网络抓取、0 条写入，原来源名称保留。
- 新版发布补跑时，将已有 20 分钟上限的原始数据缺口任务排在长耗时供应商任务之前；不增加并发、不新增通道。日常调度和发布都仍使用既有组件。
- 未将这些定位和测试算作全部数据齐全；9 月 3 日资金流、刷新后的新增证券覆盖、指数行情实际写入仍需最终回读。

## 2026-09-05 七项采集修复（发布前集中 review）

- 指数目录兼容完整国金 QMT 的真实来源标识，代码、批次与权限绑定保留；ETF 仅涨跌额和涨跌幅允许负值，价格及有限数值检查保留。
- 财务空日期先过滤再比较，不生成披露日期；龙虎榜生产版本从现有受控发布环境和准确 release 路径读取，不依赖服务账户的 Git 所有权信任。
- 换手率复用已完成且与当前行情一致的原始证据，不因应用发布重新采集。原采集版本保持不变，另记当前验证版本。生产只读重放成功恢复 5,546 条，未写数据库。
- 资金流每路请求最多 20 秒、子进程等待 25 秒；只尝试两个同源路径，保持 TLS 证书校验。每轮抓取预算 600 秒，最多等待已有在途请求完成；单次补数任务 20 分钟，全天补数重试窗口仍为 24 小时。按日期保留断点，每 100 个完成项、30 秒或本轮结束保存；每次修正前备份独立保存，避免重跑覆盖。先补独立资金流，不等待慢板块抓取。
- 巡检逐项限时 8 秒，记录实际耗时并返回其他已完成项；只读超时线程不阻止 CLI 退出。readiness 只读深度检查在可终止子进程中运行，30 秒上限，页面最多等待 0.5 秒；15 秒缓存过期后显示检查中，不冒充可用。没有新增服务、框架或重写调度器。
- 定向回归：260 passed、3 skipped。覆盖负涨跌幅、日期空值、版本跨发布复用、资金流断点恢复与进程终止、readiness 超时与过期状态。单股线上只读资金流抽样成功取得 9 月 3 日数据，不能等同于全市场补齐。
- 截至本记录，这批代码尚待合并部署。分钟历史、缺失正式财报证据及其他历史字段缺口不因代码测试通过而算作完成；连续交易日自动交付仍需实际运行证据。

2026-09-05 实际发布发现：Linux 已给版本 11c0461 签发最终授权，但发布期间 main 又合入独立游戏目录，旧 Windows 更新器只检查最新 main，因新 tip 没有发布请求而滞留旧代码。另一个独立原因是生产 checkout 的本地空代理配置导致 GitHub 连接重置；已将该 checkout 的代理恢复为本机已验证可用的代理，不修改全局配置。

最小修复：

- 从现有受保护账本的全局最新 hold 选择更新目标，不新增数据库表或服务。
- 切换前确认目标属于 origin/main，并可从当前 checkout 快进；合并准确 SHA，保留 main 中的无关提交。
- 目标与当前 SHA 相同或已授权恢复旧版本时，不依赖 GitHub fetch；写权限仍须完整 schema seal、真实 activation grant 和 QMT/bootstrap 校验。
- 第一次兼容安装允许读取真实旧协议 hold/grant；未有最终 grant 时仅停止并等待，不覆盖旧 checkout。出现过受保护恢复 context 后禁止退回旧协议。

隔离测试覆盖真实 PowerShell 选择/切换片段和 SQLite 账本状态，包括 main 已前进、兼容等待、最终授权、ABORT 选择旧版、缺少 context 及错误主机/checkout。测试不等同于生产 MySQL 权限或 QMT 运行证明。

2026-09-05 15:33 两端已实际运行 d52a79b，Windows 完整回执 READY、两个执行器各只有一个新鲜心跳。下一版将 QMT_EDGE_RECOVERY_COMPATIBILITY_INSTALL 设为 0，使用此前已安装的可信 controller 记录窄范围 pre-cutover 恢复上下文。该机制不涵盖不兼容 schema 变更后的任意回滚。

同日生产 review 还确认 Linux 普通采集并发实际为 1，公告任务占用唯一槽位，补数排队。服务器约 3.5 GB 内存、1.9 GB 可用、swap 未使用。现有 Linux 调度器启动配置固定为 2 个普通采集槽位（原代码默认也是 2），只覆盖陈旧的单并发环境值；不增加服务或调度层，不改变各数据发布器的互斥锁、任务审计及既有专用通道。增加一个槽位会增加峰值内存与外部请求并发，部署后仍须观察资源和真实入库情况。

9 月 3 日日线已通过现有按日期 BigQMT 发布器补齐：5,548 条行情、8 只原生无交易记录，分区回读 PASS。资金流缺口及分钟级历史不能由这项结果冒充完成。GitHub 连接曾间歇性重置；系统 TLS 后端诊断并未证明可解决，已撤销该临时设置，未降低证书校验。新更新器的已安装版本重试不再依赖 GitHub。

## 2026-09-05 16:40 验收记录（尚未全部完成）

- 两端已部署合并后的 `c8a86ff1a9a47e8917d2b6ea3fbea27c09e2fd28`。Linux/API 与 Windows 更新器均成功结束，Windows 独立完整回执 READY；两个执行器的当前心跳属于同一版本，普通采集并发均为 2。
- 本次真实发布经过受保护的停机请求、Windows 保留旧 checkout 等待、Linux 零写入器确认、切换、最终授权与 Windows 恢复。没有做生产故障注入，不能据此声称不兼容 schema 迁移后也可回滚。
- 最新发布边界与恢复回归：189 passed、7 skipped。此前 QMT 日线发布/历史补数回归：48 passed。测试组有重叠，不累计为一个总通过数。
- 近 21 个交易日日线整日缺口已消除；9 月 4 日资金流支持范围为 5207/5207。9 月 3 日资金流最后回读仍为 0，自动任务 116 自 16:08 起运行，不能将“运行中”算作补数成功。部署用户没有读取受保护补数断点文件的权限，未绕过该限制。
- 财务任务实际暴露四只股票的 `Cannot compare NaT with datetime.date object`。新增回归先复现四例失败，最小修复只把已有非空日期筛选移至日期比较之前；空值仍原样保留，未来日期继续被拒绝，不改变报告期或 PIT 证据规则。财务/PIT 回归 65 passed、1 skipped。**这项后续补丁尚未部署**，不为它中断当前补数。另有一只股票缺少所需报告期的正式非披露证据，不以代码兜底冒充解决。
- 通过现有浏览器登录态可打开生产今日策略页（不再返回 502），真实交易显示固定关闭。页面初始处于 LOADING，后续读取两次控制超时，因此原截图 readiness 超时尚未通过完整页面验收。
- 分钟历史、历史涨跌停字段及不可重建的 PIT 历史仍分别保留自身约束。全量稳定性验收尚未通过，不能宣称全部数据已齐或已经证明连续交易日稳定运行。
